document.addEventListener('DOMContentLoaded',()=>{document.querySelectorAll('.dashboard-grid table').forEach(table=>{Array.from(table.querySelectorAll('thead th')).forEach(header=>{header.style.width=`${Math.max(90,Math.round(header.getBoundingClientRect().width))}px`;const resizer=document.createElement('span');resizer.className='column-resizer';resizer.setAttribute('aria-hidden','true');header.appendChild(resizer);resizer.addEventListener('pointerdown',event=>{event.preventDefault();const startX=event.clientX,startWidth=header.getBoundingClientRect().width;document.body.classList.add('is-resizing');resizer.setPointerCapture(event.pointerId);const resize=e=>header.style.width=`${Math.max(90,startWidth+e.clientX-startX)}px`;const stop=()=>{document.body.classList.remove('is-resizing');resizer.removeEventListener('pointermove',resize);resizer.removeEventListener('pointerup',stop);resizer.removeEventListener('pointercancel',stop)};resizer.addEventListener('pointermove',resize);resizer.addEventListener('pointerup',stop);resizer.addEventListener('pointercancel',stop)})})})});
